spring:
  datasource:
    url: jdbc:h2:mem:portfolio_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver

app:
  jwt:
    secret: test-only-secret-0123456789abcdef0123456789abcdef
  security:
    cookie-secure: false
  storage:
    provider: local
  admin:
    email: admin@test.local
    password: Test@123
